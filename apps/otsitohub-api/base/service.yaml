apiVersion: v1
kind: Service
metadata:
  name: otsitohub-api
  labels:
    app.kubernetes.io/name: otsitohub-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: otsitohub-api
